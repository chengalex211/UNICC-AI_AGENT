[pytest]
testpaths = Expert1/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: fast unit tests (no LLM API calls)
    integration: requires live LLM API key
    live: requires a running target server
